# backend 目录说明

## 文件夹功能

保存 Python 后端服务、游戏运行时逻辑、配置、脚本和测试。

## 文件夹内容

- `src/`: 正式后端代码。
- `config/`: 运行配置与角色配置。
- `data/`: 本地运行数据。
- `scripts/`: 跑测、诊断和维护脚本。
- `tests/`: 自动化测试。
- `logs/`: 本地日志，不作为工程口径。

服务入口是 `src/main.py`，模块说明见 `src/README.md`。
