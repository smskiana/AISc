# Application 模块

## 文件夹功能

负责后端运行时装配和跨领域用例编排。

## 文件夹内容

包含系统初始化、消息总线接线、游戏循环和跨模块流程。领域细节应保留在 memory、npc、dialogue、world 等模块。

`world_preparation.py` 是开局与跨日世界准备的唯一编排入口：负责阶段进度、幂等、稳定失败原因和“日计划完成才可操作”的成功条件；运行时只负责协议接线。
