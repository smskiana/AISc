> 设计方案: 本次为规则文档小修，按约定未单独创建 plan。

# AI 上下文读取减量规则执行记录

## Workstream

ProjectGovernance

## Roadmap item

GOV：项目治理

## 需求理解

用户指出当前规则会让 AI 在大范围改动前读取过量代码和旧文档；旧文档多数只承担留档功能，不应继续作为默认指导材料。

## 实际改动

1. 更新 `AGENTS.md` 顶部设计文档说明：
   - 将“核心设计文档”改为“设计文档定位（按需读取）”。
   - 明确旧设计文档默认是留档和追溯资料，不是启动必读清单。
   - 为每份高频设计文档补充按需读取条件。
2. 更新 `AGENTS.md` 的第 0 节：
   - 将“核心开发文档”改为“上下文读取策略（少读优先）”。
   - 明确 ProjectIndex、Roadmap、Workstream、ADR、ChangeIndex 都是定位入口，不等于全文必读。
   - 将 handoff 改为非默认必读，只在接续任务或索引不清时读取。
   - 将历史 plan / execution 限制为最相关的最近 1-3 份。
   - 明确代码阅读应从 `rg` 和最小入口开始，按测试失败或契约不清再扩展。
3. 更新 `AGENTS.md` 的实际修改文档规则：
   - 修改“开始任何修改前”的检查要求，强调只做轻量索引检查，只读取命中的相关条目。

## 验证方式

1. 静态检查 `AGENTS.md` 已出现“上下文读取策略（少读优先）”。
2. 静态检查 `AGENTS.md` 已明确“handoff 不再默认必读”。
3. 本轮只修改协作规则文档，不涉及代码编译。

## 未完成项

1. `docs/ProjectIndex.md` 与 `docs/Roadmap.md` 仍保留较长的新会话阅读顺序；本轮先由 `AGENTS.md` 覆盖 AI 行为，后续如仍触发过读，可再单独收敛索引文档措辞。
2. `CLAUDE.md` 仍保留更旧的“会话启动必读”口径；本轮为避免扩大范围，未同步该入口。
