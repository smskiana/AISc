> 设计方案: [2026-07-19_AI测试工具发现与三会话交付流程_plan.md](2026-07-19_AI测试工具发现与三会话交付流程_plan.md)
>
> 执行记录: [2026-07-19_AI测试工具发现与三会话交付流程_execution.md](2026-07-19_AI测试工具发现与三会话交付流程_execution.md)

# AI 测试工具发现与三会话交付流程测试记录

## 1. 测试结论

通过。本测试在 Codex 重启后的独立会话完成，plan 的 7 条验收标准全部满足；本次项目治理改动可以认定整体完成。

## 2. 测试环境

- 项目路径：`F:/GameProject/unity/AISc`
- 日期：2026-07-19
- 会话阶段：第三个独立测试会话
- 工作区状态：保留用户既有未提交业务改动，只验证本主题治理文档

## 3. 工具选择

本任务只改变规则、索引和 README，不涉及 Unity 运行时、场景资产或 Python 业务逻辑。根据 `docs/Workstreams/TestingAndDiagnostics/README.md` 的分级原则，选择：

1. 项目索引与 Workstream 的冷启动发现检查。
2. `rg` 聚焦检查规则措辞、证据命名和顶层规则的信息泄漏。
3. `backend/scripts/check_project_conventions.py` 项目约定检查。
4. `git diff --check` 文本格式门禁。
5. Markdown 本地链接与 plan / execution / test 三向互链检查。

未读取或运行 Unity Test Runner、pytest 业务套件、长跑脚本、`aisc_debug`、`aisc_control` 和 Unity MCP；这些工具不能增加本轮纯文档治理的有效证据，全部运行会违背按需选择原则。

## 4. 验收结果

### 4.1 冷启动可发现性

通过。重启后从 `AGENTS.md` 的“测试工具发现”硬约束进入 `docs/Workstreams/TestingAndDiagnostics/README.md`；`docs/ProjectIndex.md` 的功能表也提供同一入口，无需依赖 handoff 或按日期查找记录。

### 4.2 分级工具选择

通过。测试与诊断 Workstream 能按 Unity 纯逻辑、真实 Play Mode、Python 逻辑、长跑性能、结构化状态、白名单控制、Console 和资产层问题路由到对应叶子 README。本测试只选择文档静态检查，没有预读全部叶子说明。

### 4.3 三会话与状态边界

通过。`AGENTS.md` 明确 plan、execution、test 分属三个独立对话；execution 仅允许“实现完成，待独立测试”或“实现未完成”，独立 test 通过后才能认定整体完成。`CLAUDE.md` 同步相同核心口径。

### 4.4 证据命名与互链

通过。`ProjectNamingAndIndexing.md` 定义 `_plan.md`、`_execution.md`、`_test.md` 稳定命名；本主题三份记录已互相链接并位于主要功能域 `ProjectGovernance/`。

### 4.5 未验收前禁止完成声明

通过。execution 保持“实现完成，待独立测试”，并明确在 test record 创建前不得宣称整体完成；上一执行会话的最终回复也遵守该边界。

### 4.6 自动与静态门禁

通过：

- `python backend/scripts/check_project_conventions.py`：shared ID、profile 主键和 `location_positions` 一致。
- `git diff --check`：通过，仅输出工作区既有 LF/CRLF 转换提示，无 whitespace error。
- 顶层规则工具细节检查：`AGENTS.md` / `CLAUDE.md` 未出现 `run_7day_benchmark`、`tune_memory_route_profiles`、`evaluate_deep_retrieval`、具体 snapshot 或 probe 名称。
- Markdown 本地链接检查：本测试文件创建后，主题三向互链和本轮治理入口均存在。

### 4.7 本治理流程自证

通过。第一会话创建 plan，用户明确要求后同会话例外进入实现并创建 execution；Codex 重启后，本独立测试会话读取 plan / execution、按 Workstream 选择工具并创建 test record。执行和测试阶段已明确分离。

## 5. 未覆盖项

无阻塞未覆盖项。Unity、后端业务和运行时诊断工具未运行属于按风险排除，不影响纯文档治理结论。
