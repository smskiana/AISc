> 设计方案: 本次为单文件规则消歧小修，按约定未单独创建 plan。

# 唯一方案来源规则执行记录

## Workstream

ProjectGovernance

## Roadmap item

GOV：项目治理

## 需求理解

消除“只有实际实现修改才记录 plan”与“复杂方案、执行分属两个对话”之间的歧义，防止方案对话和执行对话分别创建或制定两份方案。

## 实际改动

1. 将 plan 的适用条件明确为“已确认将进入实际实现”，继续排除未确认实施的讨论和方案草案。
2. 明确预计达到文件数量阈值时，由方案对话创建唯一 `_plan.md`。
3. 明确执行对话只引用该 plan，不得重新规划或创建第二份 plan。
4. 原方案无法执行、边界变化或用户要求调整时，必须停止执行并在独立方案对话中更新原 plan。

## 索引与口径回写

1. ProjectGovernance Workstream 中“纯方案草案不创建 plan”仍适用于未确认实施的方案，与新规则一致，无需修改。
2. 本次未改变文档目录边界或架构决策，不需要修改 ADR、ChangeIndex 或功能入口。

## 验证方式

1. 静态检查规则同时包含“唯一 plan”“执行对话不得重新制定”“更新原 plan”三个约束。
2. 本轮只修改项目治理文档，不涉及 Unity 运行时功能，因此 `aisc_debug` 不适用。

## 未完成项

无。
