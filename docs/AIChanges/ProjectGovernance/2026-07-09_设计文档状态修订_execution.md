> 设计方案: [2026-07-09_设计文档状态修订_plan.md](2026-07-09_设计文档状态修订_plan.md)

# 设计文档状态修订 — 执行记录

## 完成时间
2026-07-09

## 实际改动清单

### 修改文件 (1)

| 文件 | 实际改动 |
|------|------|
| `docs/DesignDocs/MemoryArchitecture.md` | 末尾追加“2026-07-09 设计状态修订注记”，不删除原文，仅补充当前执行口径 |

### 新建文档 (2)

| 文件 | 说明 |
|------|------|
| `docs/AIChanges/ProjectGovernance/2026-07-09_设计文档状态修订_plan.md` | 本次文档修订方案 |
| `docs/AIChanges/ProjectGovernance/2026-07-09_设计文档状态修订_execution.md` | 本执行记录 |

## 写入的核心修订内容

### 1. 反思的实现口径调整

- 保留“反思”作为设计术语
- 但在实现层面，明确改为：
  - 反思属于融合 / 图演化流程的一环
  - 不再要求独立模块、独立文件、独立交付追踪

### 2. 印象系统的工程边界调整

- 继续保留“从图派生”的设计原则
- 不再强求必须存在独立 `impression.py`

### 3. 后端未实现项优先级重排

已按用户给出的新判断写入主设计文档：

- 多 NPC 同时对话：要做
- 玩家选项生成器：要做
- 天气系统：保留，但优先度不高
- 流言 / 传闻：暂时搁置
- 独立天气模块：不急
- API 分层：先留接口
- 测试体系：原型跑通后补
- 图可视化调试工具：原型跑通后补

### 4. Unity 侧执行口径补充

主设计文档已明确：

- 未明确降级的 Unity 缺口，默认仍然要做
- 当前优先补原型闭环所需能力：
  - HUD / 时间天气显示
  - 睡眠表现
  - 存档 / 读档界面
  - 对话状态管理层
  - 共享配置同步

## 修改方式说明

本次严格遵守“不删减文档”的要求：

- 未覆盖旧章节
- 未删除旧表述
- 仅在文档末尾追加新的阶段性修订注记

这样做的好处是：

1. 保留最初设计讨论的上下文
2. 避免旧会话结论丢失
3. 让后续开发能明确区分“原始设计”与“当前执行口径”

## 验证方式

- [x] 已确认主设计文档新增内容位于末尾追加区域
- [x] 未删除原有章节、表格和讨论记录
- [x] 已把“继续做 / 降优先级 / 暂缓 / 仅保留接口”的判断写成明确文字

## 我补充的一点判断

用户把“反思”并入“融合”这个方向，我认为在当前原型阶段是合理的，能减少过早拆模块带来的复杂度。

但我没有直接抹掉文档里的“反思”术语，而是改成“保留术语、收敛实现边界”。
这样既能尊重原始设计，也能避免未来回看文档时出现语义断层。
