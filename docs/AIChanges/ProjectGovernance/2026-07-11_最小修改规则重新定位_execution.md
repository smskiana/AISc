> 设计方案: [2026-07-11_最小修改规则重新定位_plan.md](2026-07-11_最小修改规则重新定位_plan.md)

# 最小修改规则重新定位 — 执行记录

## 完成时间

2026-07-11

## 本次完成内容

已将“最小修改”从泛用默认原则，重新定位为只在特定场景下强制生效的规则，并同步修正了“侵入控制”的适用口径：

1. `最小修改` 现在明确只在修 bug / 追加小功能 / 局部修正时强制生效
2. 新系统设计、结构治理、长期扩展场景不再被它反向约束
3. `侵入控制` 也同步改为区分“修补场景”和“设计场景”
4. 这样可以避免“最小修改”与“类减重与扩展性”继续互相打架

## 实际改动清单

### 修改文件 (2)

| 文件 | 实际改动 |
|------|------|
| `AGENTS.md` | 重写第 1 条“最小修改”与第 5 条“侵入控制”的适用边界 |
| `CLAUDE.md` | 同步重写同口径规则，保持与 `AGENTS.md` 一致 |

### 新建文件 (2)

| 文件 | 说明 |
|------|------|
| `docs/AIChanges/ProjectGovernance/2026-07-11_最小修改规则重新定位_plan.md` | 本次规则调整方案 |
| `docs/AIChanges/ProjectGovernance/2026-07-11_最小修改规则重新定位_execution.md` | 本执行记录 |

## 写入的核心规则

1. `最小修改` 是“修 bug / 小功能强制原则”
2. 在局部修补场景中，必须优先小步修改，减少无关影响
3. 该原则不用于约束：
   - 新系统设计
   - 系统级整理
   - 结构治理
   - 用户明确同意的大改
4. 上述设计场景应优先服从：
   - 职责边界清晰
   - 类减重
   - 未来扩展性
5. `侵入控制` 也同步区分：
   - 修补场景：优先少侵入
   - 设计场景：不为了少改而继续把职责塞进旧结构

## 验证方式

- [x] 已确认 `AGENTS.md` 与 `CLAUDE.md` 新规则文本一致
- [x] 已确认第 1 条和第 5 条已同时修正，避免继续互相冲突
- [x] 已确认“最小修改”和“类减重与扩展性”的分工边界更清楚

## 未完成项

- 暂无
