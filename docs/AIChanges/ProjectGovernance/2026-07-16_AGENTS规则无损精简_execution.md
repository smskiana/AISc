> 设计方案: 本次为单文件规则文档精简，按约定未单独创建 plan。

# AGENTS 规则无损精简执行记录

## Workstream

ProjectGovernance

## Roadmap item

GOV：项目治理

## 需求理解

在绝不丢失现有规则语义、强制等级、触发条件和例外边界的前提下，降低 `AGENTS.md` 的重复和阅读成本。

## 实际改动

1. 合并“项目内容定位”与“上下文读取策略”，保留完整最小读取链、按需扩展条件和禁止按日期导航的要求。
2. 压缩核心行为规则措辞，将诊断钩子长段落拆为诊断、同步和完成条件三个短条目。
3. 合并 plan / execution 的适用范围、触发阈值、内容要求和互链规则，保留全部目录与会话分离约束。
4. 将原“即使是不是对应情况”的歧义表述明确为“无论是否触发减重，都必须预留职责边界、扩展位或可替换接口”。
5. 保留 Unity MCP 不可用时停止修改、类减重请求、中文交付、注释保护、抽象要求、文档同步和错误预防入口等全部约束。

## 索引与口径回写

1. `docs/Workstreams/ProjectGovernance/README.md` 已覆盖规则维护职责，无需新增入口。
2. 本次未改变项目治理边界或既有决策，不需要修改 ADR。
3. `docs/AIChanges/ProjectGovernance/README.md` 已覆盖项目规则 execution，无需登记日期文件。
4. `CLAUDE.md` 仍是过期入口，本次按用户指定范围未同步；其口径分叉仍需后续单独处理。

## 验证方式

1. 对精简前后规则逐项核对，确认定位顺序、硬约束、触发阈值、例外条件和交付门槛均有对应表述。
2. 检查 Workstream、ADR 与 ChangeIndex 的相关口径，未发现需要同步修改的功能入口。
3. 本轮只修改项目治理文档，不涉及 Unity 运行时功能，因此 `aisc_debug` 不适用。

## 未完成项

1. `CLAUDE.md` 尚未与当前 `AGENTS.md` 同步。
