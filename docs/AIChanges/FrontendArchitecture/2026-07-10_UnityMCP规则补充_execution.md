> 设计方案: 本次为规则文档小修，按约定未单独创建 plan。

# Unity MCP规则补充 — 执行记录

## 完成时间

2026-07-10

## 需求理解

用户希望补充一条新的项目规则：

- 对于场景、预制体、层级组织、编辑器生成这类问题
- 若可以通过 Unity MCP 在编辑器资产层解决
- 就不要把这些内容写死在代码里面
- 目标是保持 MVC 架构干净，避免把资源配置问题误塞进运行时逻辑

## 实际改动清单

### 修改文件 (2)

| 文件 | 实际改动 |
|------|------|
| `AGENTS.md` | 在“核心行为底线”中新增“Unity MCP优先”规则，并顺延编号 |
| `CLAUDE.md` | 同步新增“Unity MCP优先”规则，保持与 `AGENTS.md` 一致 |

### 新建文档 (1)

| 文件 | 说明 |
|------|------|
| `docs/AIChanges/FrontendArchitecture/2026-07-10_UnityMCP规则补充_execution.md` | 本次规则补充执行记录 |

## 写入的核心规则

1. 能由 Unity MCP 完成的场景与预制体问题，优先在 Unity 编辑器资产层解决
2. 不把本应属于场景配置、Prefab 配置、层级组织的问题硬编码进运行时代码
3. 这样做是为了维持 MVC 分层干净，减少“代码代替资源配置”的结构污染
4. 只有 Unity MCP 无法稳定完成，或需求本质上属于运行时逻辑时，才进入代码实现

## 验证方式

- [x] 已确认 `AGENTS.md` 新增“Unity MCP优先”条款
- [x] 已确认 `CLAUDE.md` 与 `AGENTS.md` 文本一致

## 未完成项

- 暂无
