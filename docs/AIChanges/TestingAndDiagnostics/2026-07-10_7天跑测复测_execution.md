# 7 天跑测复测 — 执行记录

## 完成时间
2026-07-10

## 本次做了什么

在“跑测问题修复”落地后，按和上一轮一致的口径重新跑了一次完整 7 天后端长跑压测，目的是看这轮修复到底带来了什么变化。

这次复测特点：

1. 沿用“按游戏分钟手动推进”的后端长跑方式
2. 保留完整行为、社交、对白、午夜提取、图演化、夜间印象结算链路
3. 不污染主库，改为使用隔离测试库和隔离 LanceDB 目录

## 运行产物

| 文件 | 说明 |
|------|------|
| `docs/AIChanges/artifacts/2026-07-10_7day_full_test_rerun/2026-07-10_7day_full_test_rerun.log` | 本轮复测原始控制台日志 |
| `docs/AIChanges/artifacts/2026-07-10_7day_full_test_rerun/2026-07-10_7day_full_test_rerun_summary.json` | 本轮复测的 UTF-8 清洁摘要 |
| `docs/AIChanges/artifacts/2026-07-10_7day_full_test_rerun/` | 隔离测试库、LanceDB、临时存档目录 |

## 执行方式

本轮没有启动 Unity，而是在 `pwsh` 中直接运行后端复测脚本，流程如下：

1. 重定向 `DATA_DIR / SAVE_DIR` 到 `docs/AIChanges/artifacts/2026-07-10_7day_full_test_rerun/`
2. 启动 [backend/src/application/runtime.py](F:/GameProject/unity/AISc/backend/src/application/runtime.py)
3. 执行 `cold_start()`
4. 从 `第1天 08:00` 开始，按 `7 * 1440` 游戏分钟逐分钟推进
5. 每次命中 `24:00` 记录午夜前后图规模、短期记忆数量和午夜耗时
6. 结束后导出最终图规模、边类型分布、短期记忆数量和检索样本

## 结果摘要

### 1. 总耗时

- 上一轮：`1287.72s`
- 本轮：`1280.74s`

结论：

1. 总耗时基本持平，略快约 `6.98s`
2. 这说明这轮修复没有把整轮长跑拖慢

### 2. 午夜耗时

- 上一轮平均：`26.42s`
- 本轮平均：`28.23s`
- 上一轮最大：`30.91s`
- 本轮最大：`35.41s`

结论：

1. 午夜没有失控，但平均和峰值都略有上升
2. 这和夜间印象生成、情绪补强、检索结构补强带来的额外工作量一致
3. 当前“记忆质量提升”是明显的，但午夜性能还不算优化完成

### 3. 图规模变化

#### 总量

- 上一轮节点总数：`485`
- 本轮节点总数：`425`
- 上一轮边总数：`380`
- 本轮边总数：`438`

结论：

1. 节点减少、边增加
2. 这符合“复用更多、连接更密”的预期
3. 图整体比上一轮更紧，不再那么像“堆节点”

#### 各 NPC 图规模

| NPC | 上一轮 nodes/edges | 本轮 nodes/edges |
|------|------|------|
| sakura | `73 / 50` | `82 / 102` |
| chihaya | `152 / 121` | `111 / 119` |
| kazuha | `70 / 64` | `81 / 67` |
| tatsunosuke | `142 / 109` | `108 / 105` |
| kujo | `48 / 36` | `43 / 45` |

结论：

1. `chihaya`、`tatsunosuke` 之前那种明显偏高的节点膨胀被压下来了
2. `sakura` 的边数明显上升，说明“和熟人相关的事件”更容易被接回图
3. 整体分布比上一轮更均衡

### 4. 边类型分布

- 上一轮 `felt = 8`
- 本轮 `felt = 81`

完整分布（本轮）：

| type | count |
|------|------:|
| involved | 128 |
| felt | 81 |
| located_at | 81 |
| mentioned | 72 |
| happened_at | 51 |
| relationship | 25 |

结论：

1. `felt` 从 `8` 提升到 `81`，这是这轮最明显的改进
2. 情绪边已经不再是装饰性稀疏边
3. 之前“印象/状态缺图中情绪支撑”的问题明显缓解

### 5. 短期记忆保有量

- 上一轮：
  - `sakura 28`
  - `chihaya 24`
  - `kazuha 23`
  - `tatsunosuke 20`
  - `kujo 10`
- 本轮：
  - `sakura 26`
  - `chihaya 21`
  - `kazuha 19`
  - `tatsunosuke 17`
  - `kujo 8`

结论：

1. 短期记忆仍在稳定保留和清理
2. 没有出现异常膨胀
3. 数量略低，和这轮图结构更积极吸收、夜间处理更完整是相符的

### 6. 检索样本

本轮复测后的检索样本已经明显更贴“当前目标人物相关事件”。

示例：

1. `sakura -> chihaya`
   - 已经主要回忆“千早送面包、来花店、一起互动”的事件
2. `chihaya -> sakura`
   - 已经主要回忆“给鹿岛樱带新面包、在花店聊天、互动细节”的事件
3. `kazuha -> tatsunosuke`
   - 仍保留核心背景记忆，但也开始夹带近期具体互动

这和上一轮“旧高重要事件总抢前排”的现象相比，方向是对的。

## 当前判断

这轮复测说明修复是有效的，而且效果不是只停留在定向 smoke：

1. 熟人检索结果明显更贴目标人物
2. 图结构更像“少造点、多连边”
3. `felt` 情绪边已经真正进图

同时也暴露了新的事实：

1. 午夜性能没有崩，但确实比上一轮更重了
2. 下一步如果继续优化，优先级应回到“夜间并发和缓存”而不是再改记忆语义本身

## 备注

1. 本轮原始控制台日志仍夹带少量终端层乱码；分析请优先看 `2026-07-10_7day_full_test_rerun_summary.json`
2. 这次复测没有修改项目代码，只新增了测试产物和这份执行记录
