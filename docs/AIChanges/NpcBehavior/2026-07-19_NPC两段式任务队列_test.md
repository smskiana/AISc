# NPC 两段式任务队列阶段 6 独立测试记录

> 执行方案：[2026-07-19_NPC两段式任务队列_plan.md](2026-07-19_NPC两段式任务队列_plan.md)
>
> 累计执行记录：[2026-07-19_NPC两段式任务队列_execution.md](2026-07-19_NPC两段式任务队列_execution.md)

## 测试结论

阶段 6 当前为部分通过，不能切换为“整体上线验收完成”。首次测试发现的跨日 `PreparingUnity`、暂停源未释放和当前 schema 写入拒绝已在修复后真实复测通过；day 2 已解除暂停、存档回到 `Idle` 并成功推进任务。现有白名单没有同日读档 action，也没有玩家 append/preempt/defer/reject 的真实 producer probe，这两项仍缺 PlayMode 证据。

## 环境与工具选择

1. 日期：2026-07-19；Unity 2022.3.62t7；场景 `Town_Main`；Test 时间档序列化值为 1200 秒。
2. 使用阶段 5 新鲜 codebase-memory 索引 `AISc_two_segment_stage5_20260719`（6372 nodes / 20374 edges）做符号、测试覆盖和旧入口审计；本轮没有代码变更，不重复建立等价索引。
3. Python 使用聚焦 pytest，覆盖 planner 双队列、catalog 分段、校验、fallback、幂等和超时契约。
4. Unity 使用聚焦 EditMode、`aisc_control.run_daily_schedule_probe`、`aisc_debug` 结构化快照和 Console。真实 Play Mode 只执行新游戏、day 1 与跨日 day 2 主链；发现阻断后停止扩大范围。

## 结构化证据

### Python 与静态门禁

1. `python -m pytest backend/tests/test_daily_schedule_planner.py backend/tests/test_npc_task_semantics.py -q`：15 passed，0 failed，耗时 0.29 秒。
2. `python -m compileall -q backend/src/npc backend/src/application/runtime.py backend/src/main.py`：通过。
3. `git diff --check`：通过，仅输出现有 LF/CRLF 提示。
4. `rg` 确认 `Assets/Scenes/Town_Main.scene` 中 `_testDayRealSeconds: 1200`。
5. `NPC_SCHEDULE_REPLAN_REQUEST` 无生产代码命中；`schedule_window_expired` 只存在于无生产 caller 的兼容策略壳 `DefaultNpcScheduleSwitchPolicy`。

### Unity EditMode 与隔离 probe

1. 筛选 `NpcDailyScheduleTests`、`GameCommandSenderTests`、`AiscDiagnosticsTests`：37 passed / 0 failed / 0 skipped，耗时 0.688 秒。
2. `jump_to_17`：通过，`decision=work`，`reason=work_task_continues_after_segment`，证明 17:00 活动工作可续跑。
3. `social_lock_defer`：通过，`decision=deferred`，`reason=npc_social_dialogue_locked`。
4. `task_failure_replan`：通过，`reason=task_terminal_consumed`，失败终态消费当前项且未触发整表 replan。
5. `late_revision`：通过，`decision=rejected`，`reason=stale_plan_revision`。
6. `fixed_input_planner`：通过，`status=success`，5 个候选全部入选，生命周期动作被稳定排除。
7. `provider_timeout`：通过，`status=fallback`，5 个 fallback 项，`failure_reason=provider_timeout`，且 trace 标记 provider call 未被错误取消。

### 真实 Play Mode

1. 新游戏完成初始世界准备：连接和协议协商成功，`world_preparation.phase=complete`，5 个 NPC 实体和 5 份 `daily_schedule_v2` day 1 计划可见。
2. day 1 任务证据：`sakura` 与 `kazuha` 的 `work_arrange` 真实任务进入 `succeeded`；对应 `day_plan_snapshot.last_terminal_reason=succeeded`，活动工作项已消费。
3. 午夜维护成功：`midnight_snapshot.status=success`、`phase=complete`、5 个 owner 印象均成功，耗时 18.189 秒。
4. day 2 计划已原子换代：5 个快照均为 `game_day=2`、`plan_revision=1`、`planner_version=daily_schedule_v2`，没有 V1 owner 证据。
5. 阻断证据：跨日请求后至少 65 秒，`world_preparation` 已 complete，但 `save.phase=PreparingUnity`，`loading_overlay` 与 `sleep_flow` 仍持有 Gameplay/FX 暂停，时间停在 day 2 06:00。
6. Console 同期出现一条警告：`[WS] 消息分发失败: 只能写入当前 Unity 存档 schema`，调用点为 `WebSocketClient.DrainIncomingMessages`。该证据与存档阶段悬挂相关，根因须在新的执行会话诊断。

## 未覆盖项

1. 因跨日存档阻断，未继续验证同日读档、跨日读档恢复和完整 day 2 Gameplay 推进。
2. 未完成真实 NPC-NPC 暂停/恢复、玩家 append/preempt/defer/reject 四类 mutation 长链。
3. 未等待现实 1200 秒完整跑完 day 1；17:00 与失败/取消由隔离 probe 和 EditMode 覆盖，真实日内边界仍需复测。
4. 未取得“每 NPC 恰好一次真实 provider 调用”的完整 owner trace 汇总；只确认 5 份真实 V2 计划成功生成。

## 后续要求

1. 新开执行会话诊断并修复跨日 `PreparingUnity` 悬挂及存档 schema 写入拒绝，不在本测试会话修改实现。
2. 修复后在本文件追加有界复测；复测至少覆盖跨日解除暂停、存档提交完成、day 2 任务推进、同日读档和四类玩家 mutation。
3. 复测全部通过前，不更新 Roadmap 或相关 Workstream 为“已上线”，阶段 1-5 仍只可表述为实现完成。

## 2026-07-19 有界复测：schema 3 修复后跨日链

### 复测范围

1. 使用修复后的新鲜索引 `AISc_save_schema_fix_20260719`（6374 nodes / 20408 edges）确认 `UnitySaveRepository.CurrentSchemaVersion=3`、prepare/commit/load 和迁移测试入口。
2. 重跑 Python 日计划契约、Unity 日计划/协议/诊断 EditMode，以及真实“新游戏 -> 午夜 -> day 2”原失败路径。
3. 追加真实 NPC-NPC 完成、玩家抢占和会合移动失败，核对社交终态与 day plan 不变性。

### 复测证据

1. Python：`test_daily_schedule_planner.py` 与 `test_npc_task_semantics.py` 共 15 passed；相关 `compileall` 通过。
2. Unity EditMode：筛选 `NpcDailyScheduleTests`、`GameCommandSenderTests`、`AiscDiagnosticsTests`，39 passed / 0 failed / 0 skipped；包含默认 schema 3 prepare 与 schema 2 到 3 migration 回归。
3. 新游戏：`world_preparation.phase=complete`、`save.phase=Idle`、无暂停源，5 个 `daily_schedule_v2` day 1 owner 可见。
4. 跨日：45 秒后快照为 day 2 06:36，`world_preparation.phase=complete`、`save.phase=Idle`、`active_sources=[]`、`unity_time_scale=1`；原 `PreparingUnity` 阻断未复现。
5. day 2 推进：`sakura` 的 `dayplan:2:1:5304a0835eb8ab99` 真实任务 `stand` 进入 `succeeded`，对应日计划终态被消费。
6. Console 对“只能写入当前 Unity 存档 schema”的过滤结果为 0；只有一次 Unity MCP 自身重连警告，不属于业务 Console 异常。
7. NPC-NPC `cross_location_complete` 达到 `completed`；`player_preempt` 达到 `player_dialogue_interrupted`；`rendezvous_failure` 达到 `movement_failed`。三次之后 `sakura/chihaya` 的 day 2 plan revision 均保持 1，队列计数不变。
8. `git diff --check` 通过，仅有工作区既存 LF/CRLF 提示；Test 时间档仍为 1200 秒，旧 `NPC_SCHEDULE_REPLAN_REQUEST` 无生产命中。

### 复测结论与剩余项

1. schema 版本收口修复通过真实跨日复测；存档提交、暂停释放和 day 2 任务推进均恢复正常。
2. 同日读档仍未覆盖：当前 `aisc_control` 没有 save/load 白名单 action，不能用临时代码绕过正式入口伪造验收。
3. 玩家 append/preempt/defer/reject 仍只有 EditMode/runtime 证据：当前控制工具没有真实 mutation producer probe，不能宣称生产对话链已验收。
4. 阶段 6 因上述两项保持部分通过；补齐正式控制入口或提供人工 PlayMode 结构化证据后，再在本记录追加最终复测并执行口径切换。
